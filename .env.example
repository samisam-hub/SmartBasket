# Optional for local-only testing. Use the dedicated SmartBasket backend.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Legacy fallback when a publishable key is not supplied:
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# EXPO_PUBLIC values are embedded in the APK. Never use secret/service_role keys.
